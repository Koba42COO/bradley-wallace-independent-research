apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wallace-research-ingress
  annotations:
    HOST_REDACTED_30/rewrite-target: /
    HOST_REDACTED_30/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    HOST_REDACTED_30/rate-limit: "100"
    HOST_REDACTED_30/rate-limit-window: "1m"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - wallace-research.com
    - api.wallace-research.com
    secretName: wallace-research-tls
  rules:
  - host: wallace-research.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: aiva-ide-client-service
            port:
              number: 3000
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: aiva-ide-server-service
            port:
              number: 3001
  - host: api.wallace-research.com
    http:
      paths:
      - path: /wqrf
        pathType: Prefix
        backend:
          service:
            name: wqrf-api-service
            port:
              number: 5001
      - path: /cudnt
        pathType: Prefix
        backend:
          service:
            name: cudnt-accelerator-service
            port:
              number: 8080
