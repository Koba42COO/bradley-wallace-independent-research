# chAIos Protected Runtime Dependencies
# ====================================
# Minimal dependencies for obfuscated deployment
# Source code removed, only runtime requirements

# Core runtime (minimal)
fastapi==0.100.0
uvicorn[standard]==0.23.0
pydantic==2.0.0

# Essential libraries only
numpy==1.24.0
scipy==1.11.0
python-multipart==0.0.6

# Security and monitoring
cryptography==41.0.0
python-jose[cryptography]==3.3.0

# Database (if needed)
aiosqlite==0.19.0

# Logging
structlog==23.1.0

# Note: Development dependencies removed
# Note: Source code dependencies obfuscated
# Note: No ML/AI libraries exposed in clear text
