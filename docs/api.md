# API Reference

## Endpoints

- GET /health — healthcheck
- GET /metrics — Prometheus metrics
- POST /optimize — body: { mode: string, data?: any }
- POST /entropy/validate — runs entropy validation (test payload)
